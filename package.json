{
    "name": "qat-web-task-monorepo",
    "version": "1.0.0",
    "scripts": {
        "build": "npm run build --prefix mini_project",
        "install:all": "npm install --prefix mini_project && npm install --prefix backend"
    }
}